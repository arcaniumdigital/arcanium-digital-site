import{describe,it,expect}from"vitest";import{handleRequest,type Env}from"../src/index";const env:Env={ENVIRONMENT:"test",TEST_CLIENT_IDS:"TEST-0001",A10_HMAC_SECRET:"x",MAX_ACTIONS:"25",AUTHORITY_DB:{prepare:()=>{throw Error('db')}}};describe('A10',()=>{it('disables sends/orders/LLM',async()=>{const b=await(await handleRequest(new Request('https://x/health'),env)).json()as{data:{outreach_enabled:boolean;orders_enabled:boolean;llm_enabled:boolean}};expect(b.data).toEqual({service:'arcanium-authority-control',environment:'test',outreach_enabled:false,orders_enabled:false,llm_enabled:false})});it('rejects unsigned',async()=>expect((await handleRequest(new Request('https://x/v1/opportunities',{method:'POST',headers:{'content-type':'application/json'},body:'{}'}),env)).status).toBe(401))});
