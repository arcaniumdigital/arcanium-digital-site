import{describe,it,expect}from"vitest";import{handleRequest,type Env}from"../src/index";const env:Env={ENVIRONMENT:"test",TEST_CLIENT_IDS:"TEST-0001",A7_HMAC_SECRET:"x",MAX_ACTIONS:"15",GEO_DB:{prepare:()=>{throw Error('db')}}};describe('A7 worker',()=>{it('disables spend and LLM',async()=>{const x=await(await handleRequest(new Request('https://x/health'),env)).json()as{data:{llm_enabled:boolean;provider_spend_enabled:boolean}};expect(x.data).toMatchObject({llm_enabled:false,provider_spend_enabled:false})});it('rejects unsigned input',async()=>expect((await handleRequest(new Request('https://x/v1/observations',{method:'POST',headers:{'content-type':'application/json'},body:'{}'}),env)).status).toBe(401))});
