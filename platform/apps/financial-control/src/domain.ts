export interface CloseInput { environment:"TEST"|"PRODUCTION"; approval_id?:string; reconciliation_balanced:boolean; client_delivery_enabled:boolean; }
export function authorizeClose(input:CloseInput) { if(input.environment!=="TEST")return{permitted:false,code:"PRODUCTION_CLOSE_FORBIDDEN"}; if(!input.reconciliation_balanced)return{permitted:false,code:"RECONCILIATION_REQUIRED"}; if(!input.approval_id)return{permitted:false,code:"APPROVAL_REQUIRED"}; if(input.client_delivery_enabled)return{permitted:false,code:"CLIENT_DELIVERY_DISABLED"}; return{permitted:true,code:"PERMITTED"}; }
