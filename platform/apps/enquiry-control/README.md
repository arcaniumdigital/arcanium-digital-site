# A8 enquiry-control foundation

The next A8 implementation slice will enforce verified consent, recipient
allowlisting, opt-outs, idempotent delivery keys and `ALLOW_PUBLIC_SEND=false`.
No email or SMS route is enabled by this placeholder.
