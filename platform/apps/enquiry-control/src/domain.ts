export interface DeliveryRequest { client_id:string; idempotency_key:string; channel:"email"|"sms"; recipient:string; consent_verified:boolean; opted_out:boolean; test_recipient:boolean; }
export function authorizeDelivery(r:DeliveryRequest, enabled:boolean, seen:Set<string>){if(!r.consent_verified)return{permitted:false,code:"CONSENT_REQUIRED"};if(r.opted_out)return{permitted:false,code:"OPTED_OUT"};if(r.channel==="email"?!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(r.recipient):!/^\+?[1-9]\d{7,14}$/.test(r.recipient.replace(/\s/g,"")))return{permitted:false,code:"INVALID_RECIPIENT"};if(seen.has(r.idempotency_key))return{permitted:false,code:"DUPLICATE_DELIVERY"};if(!enabled&&!r.test_recipient)return{permitted:false,code:"PUBLIC_SEND_DISABLED"};seen.add(r.idempotency_key);return{permitted:true,code:"TEST_DELIVERY_ALLOWED"};}
