import{describe,expect,it}from"vitest";import{authorizeOutreach}from"../src/domain";describe("A10",()=>it("requires evidence approval and enabled outreach",()=>{const o={domain:"example.com",entity:"Arcanium",evidence_ref:"test",approved:true};expect(authorizeOutreach(o,false,new Set()).code).toBe("OUTREACH_DISABLED");expect(authorizeOutreach({...o,approved:false},true,new Set()).code).toBe("APPROVAL_REQUIRED");}));
