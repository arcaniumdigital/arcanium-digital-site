import{describe,expect,it}from"vitest";import{authorizeDelivery}from"../src/domain";const b={client_id:"TEST-0001",idempotency_key:"k",channel:"email" as const,recipient:"test@example.com",consent_verified:true,opted_out:false,test_recipient:true};describe("A8",()=>{it("enforces consent opt-out public gate and dedup",()=>{expect(authorizeDelivery({...b,consent_verified:false},false,new Set()).code).toBe("CONSENT_REQUIRED");expect(authorizeDelivery({...b,opted_out:true},false,new Set()).code).toBe("OPTED_OUT");expect(authorizeDelivery({...b,test_recipient:false},false,new Set()).code).toBe("PUBLIC_SEND_DISABLED");const s=new Set<string>();expect(authorizeDelivery(b,false,s).permitted).toBe(true);expect(authorizeDelivery(b,false,s).code).toBe("DUPLICATE_DELIVERY");});});
