import{describe,it,expect}from"vitest";import{handleRequest,type Env}from"../src/index";const env:Env={ENVIRONMENT:"test",TEST_CLIENT_IDS:"TEST-0001",A11_HMAC_SECRET:"x",REPORT_DB:{prepare:()=>{throw Error('db')}}};describe('A11',()=>{it('disables public delivery',async()=>{const b=await(await handleRequest(new Request('https://x/health'),env)).json()as{data:{public_delivery_enabled:boolean}};expect(b.data.public_delivery_enabled).toBe(false)});it('rejects unsigned',async()=>expect((await handleRequest(new Request('https://x/v1/reports',{method:'POST',headers:{'content-type':'application/json'},body:'{}'}),env)).status).toBe(401))});
