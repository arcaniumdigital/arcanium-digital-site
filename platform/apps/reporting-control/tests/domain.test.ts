import{describe,expect,it}from"vitest";import{authorizeReport}from"../src/domain";describe("A11",()=>it("requires compact reconciled approved delivery",()=>{expect(authorizeReport({expected:1,observed:1,contains_raw_rows:true,approval_id:"a"},true).code).toBe("RAW_ANALYTICS_FORBIDDEN");expect(authorizeReport({expected:1,observed:1,contains_raw_rows:false,approval_id:"a"},false).code).toBe("PUBLIC_DELIVERY_DISABLED");}));
